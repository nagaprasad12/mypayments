/// validation types to be applied specified text field
enum Validations {
  /// when the text field is email
  email,

  /// when the text field is mobile
  mobile,

  /// when the text field is required
  requiredFieldValidator,

  ///when text field is password
  passwordFieldValidator,

  /// when nameFieldValidator
  nameFieldValidator,
}
